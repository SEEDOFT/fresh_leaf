final class AppConstants {
  static const String apiBaseUrl = 'https://api.freshleaf.dev';
  static const Duration connectTimeout = Duration(seconds: 15);
  static const Duration receiveTimeout = Duration(seconds: 20);
}
